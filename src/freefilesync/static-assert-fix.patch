--- a/zen/utf.h
+++ b/zen/utf.h
@@ -271,6 +271,9 @@
 template <class CharType>
 using UtfDecoder = impl::UtfDecoderImpl<CharType, sizeof(CharType)>;
 
+// helper for dependent static_assert (only triggers when instantiated)
+template <class T> inline constexpr bool always_false = false;
+
 
 template <class CharType, class Function> inline
 void codePointToUtf(impl::CodePoint cp, Function writeOutput) //"writeOutput" is a unary function taking a CharType
@@ -282,7 +285,7 @@
     else if constexpr (sizeof(CharType) == 4) //Linux/macOS: UTF32-wchar_t
         writeOutput(cp);
     else
-        static_assert(false);
+        static_assert(always_false<CharType>, "unsupported CharType size");
 }
 
 //-------------------------------------------------------------------------------------------
--- a/zen/basic_math.h
+++ b/zen/basic_math.h
@@ -233,6 +233,9 @@
 }
 
 
+// helper for dependent static_assert (only triggers when instantiated)
+template <class U> inline constexpr bool always_false = false;
+
 template <size_t N, class T> inline
 constexpr T power(T value)
 {
@@ -241,7 +244,7 @@
     else if constexpr (N == 3)
         return value * value * value;
     else //need more!?
-        static_assert(false);
+        static_assert(always_false<T>, "unsupported N for power()");
 }
 
 
